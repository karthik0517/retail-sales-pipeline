# This is a Databricks asset bundle definition for retail_sales_pipeline.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: retail_sales_pipeline
  uuid: db1721ff-dec6-45a2-b407-1aa8672a4422

include:
  - resources/*.yml
  - resources/*/*.yml

workspace:
  root_path: /Workspace/Shared/retail_sales_pipeline

permissions:
  - service_principal_name: retail-databricks-cicd
    level: CAN_MANAGE

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use
targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-7405608713055155.15.azuredatabricks.net
    variables:
      catalog: app_dev_catalog
      schema: bronze
  prod:
    mode: production
    workspace:
      host: https://adb-7405608713055155.15.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/karthik90.devops@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/karthik90.devops@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: app_dev_catalog
      schema: prod
    permissions:
      - user_name: karthik90.devops@gmail.com
        level: CAN_MANAGE
